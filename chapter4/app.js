// CHAPTER 1
// QUES1
var firstName = "John", lastName = "Doe", age = 25;

// QUES2
// LEGALE CASE
userName (Standard camelCase)

$amount (Can start with $)

_privateVar (Can start with _)

user_age (Can use underscores)

score1 (Can contain numbers as long as they aren't first)


    // ILLEGAL CASE
    5 Illegal Variable Names
1stUser (Cannot begin with a number)

user-name (Hyphens are not allowed; interpreted as subtraction)

user name (Spaces are strictly invalid)

let (Cannot use reserved JS keywords)

user@email (Special characters other than $ and _ are forbidden)


// QUES3


  <h1>Rules for naming JS variables</h1>
  
  <p>Variable names can only contain <strong>numbers</strong>, <strong>$ (dollar sign)</strong>, <strong>_ (underscore)</strong>, and <strong>letters</strong>. <br>
     For example <code>$my_1stVariable</code></p>
     
  <p>Variables must begin with a <strong>letter</strong>, <strong>$ (dollar sign)</strong> or <strong>_ (underscore)</strong>. <br>
     For example <code>$name</code>, <code>_name</code> or <code>name</code></p>
     
  <p>Variable names are case <strong>sensitive</strong></p>
  <p>Variable names should not be JS <strong>keywords</strong> 


  //FILL IN THE BLANKS
  b) Variable names can only contain numbers, $, _ and letters.

c) Variables must begin with a letter, $ or _.

d) Variable names are case sensitive.

e) Variable names should not be JS keywords.